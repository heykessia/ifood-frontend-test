import React from 'react';
import { Navbar, Title } from './styled';

const Header = () => {
  return (
    <div>
      <Navbar>
        <Title>Spotifood</Title>
      </Navbar>
    </div>
  );
}

export default Header;