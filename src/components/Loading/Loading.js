import React from 'react';
import { LoadingCircle, Container } from './styles';

const Loading = () => {
  return (
    <Container>
      <LoadingCircle />
    </Container>
  );
}

export default Loading;