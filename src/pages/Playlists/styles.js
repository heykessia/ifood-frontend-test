import styled from 'styled-components/macro';
import { Grid as MUGrid, Container as MUContainer } from "@material-ui/core";

export const Grid = styled(MUGrid)``;
export const Container = styled(MUContainer)``;
